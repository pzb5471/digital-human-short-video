import React from 'react';import {useCurrentFrame} from 'remotion';import type {Cue} from './types';export const Captions=({cues}:{cues:Cue[]})=>{const ms=useCurrentFrame()*1000/30;const cue=cues.find(c=>ms>=c.start_ms&&ms<c.end_ms);if(!cue)return null;const visualLines=cue.lines.slice(0,2).map((text,line)=>({text,words:cue.words.filter(w=>(w.line??0)===line)}));return <div style={{position:'absolute',bottom:260,width:'100%',textAlign:'center',fontSize:54,color:'white',zIndex:3}}>{visualLines.map(({text,words},line)=><div key={line}>{words.length?words.map((w,i)=><span key={i} style={{color:w.highlight&&ms>=w.start_ms&&ms<w.end_ms?'#ffd54a':'white'}}>{w.text}</span>):text}</div>)}</div>};
