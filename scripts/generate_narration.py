"""Local CLI placeholder; production callers must inject credentials and client configuration."""
import sys
print("Use NarrationPipeline with an injected CosyVoiceClient; no implicit provider call.", file=sys.stderr)
raise SystemExit(2)
